<%- include('partials/header') %>

<section class="checkout-page">
  <div class="container">
    <h1><i class="fas fa-lock"></i> Checkout</h1>

    <form action="/cart/place-order" method="POST" class="checkout-layout">
      <div class="checkout-form">
        <div class="checkout-section">
          <h3><i class="fas fa-truck"></i> Shipping Information</h3>
          <div class="form-group">
            <label>Full Name</label>
            <input type="text" name="shipping_name" value="<%= profile.full_name || '' %>" required placeholder="Your full name">
          </div>
          <div class="form-group">
            <label>Address</label>
            <textarea name="shipping_address" required placeholder="Street address, apartment, suite, etc."><%= profile.address || '' %></textarea>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>City</label>
              <input type="text" name="shipping_city" value="<%= profile.city || '' %>" required placeholder="City">
            </div>
            <div class="form-group">
              <label>Country</label>
              <input type="text" name="shipping_country" value="<%= profile.country || '' %>" required placeholder="Country">
            </div>
          </div>
          <div class="form-group">
            <label>Phone</label>
            <input type="text" name="shipping_phone" value="<%= profile.phone || '' %>" required placeholder="Phone number">
          </div>
        </div>

        <div class="checkout-section">
          <h3><i class="fas fa-credit-card"></i> Payment Method</h3>
          <div class="payment-options">
            <label class="payment-option active">
              <input type="radio" name="payment_method" value="cod" checked onchange="selectPayment(this)">
              <span class="payment-icon"><i class="fas fa-money-bill-wave"></i></span>
              <span>Cash on Delivery</span>
            </label>
            <label class="payment-option">
              <input type="radio" name="payment_method" value="card" onchange="selectPayment(this)">
              <span class="payment-icon"><i class="fas fa-credit-card"></i></span>
              <span>Credit/Debit Card</span>
            </label>
            <label class="payment-option">
              <input type="radio" name="payment_method" value="paypal" onchange="selectPayment(this)">
              <span class="payment-icon"><i class="fab fa-paypal"></i></span>
              <span>PayPal</span>
            </label>
          </div>
        </div>

        <div class="checkout-section">
          <h3><i class="fas fa-sticky-note"></i> Order Notes (Optional)</h3>
          <div class="form-group">
            <textarea name="notes" placeholder="Special instructions for your order..."></textarea>
          </div>
        </div>
      </div>

      <div class="checkout-summary">
        <h3>Order Summary</h3>
        <div class="checkout-items">
          <% cartItems.forEach(item => { %>
            <div class="checkout-item">
              <img src="<%= item.image %>" alt="<%= item.name %>">
              <div class="checkout-item-info">
                <span class="checkout-item-name"><%= item.name %></span>
                <span class="checkout-item-qty">Qty: <%= item.quantity %></span>
              </div>
              <span class="checkout-item-price">$<%= (parseFloat(item.price) * item.quantity).toFixed(2) %></span>
            </div>
          <% }) %>
        </div>
        <div class="summary-row">
          <span>Subtotal</span>
          <span>$<%= total.toFixed(2) %></span>
        </div>
        <div class="summary-row">
          <span>Shipping</span>
          <span class="text-success">FREE</span>
        </div>
        <hr>
        <div class="summary-row summary-total">
          <span>Total</span>
          <span>$<%= total.toFixed(2) %></span>
        </div>
        <button type="submit" class="btn btn-primary btn-block btn-lg">
          <i class="fas fa-check"></i> Place Order
        </button>
      </div>
    </form>
  </div>
</section>

<script>
function selectPayment(radio) {
  document.querySelectorAll('.payment-option').forEach(el => el.classList.remove('active'));
  radio.closest('.payment-option').classList.add('active');
}
</script>

<%- include('partials/footer') %>
